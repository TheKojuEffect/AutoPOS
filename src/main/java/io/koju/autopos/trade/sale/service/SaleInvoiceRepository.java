package io.koju.autopos.trade.sale.service;

import io.koju.autopos.kernel.service.AuditableBaseRepository;
import io.koju.autopos.trade.sale.domain.SaleInvoice;

interface SaleInvoiceRepository extends AuditableBaseRepository<SaleInvoice> {
}
