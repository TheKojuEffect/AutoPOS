autopos-dev-postgresql:
  container_name: autopos-dev-postgresql
  image: postgres:9.4.5
  # volumes:
  #   - ~/volumes/jhipster/AutoPOS/dev-postgresql/:/var/lib/postgresql/
  environment:
    - POSTGRES_USER=AutoPOS
    - POSTGRES_PASSWORD=
  ports:
    - "5432:5432"
